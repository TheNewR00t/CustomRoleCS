Este plugin de scp sl es una modificacion de exiled.customrole que arregla algunos fallos que tiene el mismo plugin

Para usarlo en vez de poner "CustomRole : CustomRole" pon "CustomRole : CustomRoleCS"
